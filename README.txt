Ghi chú cho bài tập:
+) sử dụng nền tảng là bài .c trong môn Cadvan
+) Vì sử dụng thư viện đặc biệt nên câu lệnh chạy như sau:
	gcc -g -c graph_traversal.c
	gcc -g -o graph graph_traversal.o libfdr.a
	./graph
	dot -Tpdf dothi.dot -o graph.pdf
+) dữ liệu đầu vào phải đúng form, lưu trong dothi.txt

 
